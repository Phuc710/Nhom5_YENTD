HOST=0.0.0.0
PORT=5051
SECRET_KEY=traffic-ai-enterprise-secret-key-2026

# Cấu hình MQTT (Dùng HiveMQ Public để test cho tiện)
MQTT_ENABLE=1
MQTT_HOST=broker.hivemq.com
MQTT_PORT=1883
MQTT_TOPIC_VIOLATION=traffic/iot/violation

# Database (Tự sinh ra trong folder server)
DB_PATH=./tva.db