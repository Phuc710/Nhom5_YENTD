# =============================================
# Backend Configuration Template
# Copy to .env and fill in real values
# KHÔNG commit .env này lên git!
# =============================================

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-role-key

# ThingsBoard
THINGSBOARD_URL=http://localhost:9090
THINGSBOARD_USERNAME=tenant@thingsboard.org
THINGSBOARD_PASSWORD=tenant

# MQTT (ThingsBoard broker)
MQTT_TB_HOST=localhost
MQTT_TB_PORT=1883

# MQTT (Mosquitto broker riêng)
MQTT_HOST=localhost
MQTT_PORT=1888

# Server
HOST=0.0.0.0
PORT=8000
DEBUG=true
LOG_LEVEL=INFO

# Uploads
UPLOAD_DIR=uploads
MAX_UPLOAD_SIZE=10485760

# ML Models
DETECTOR_MODEL_PATH=ml/LP_detector_nano_61.pt
OCR_MODEL_PATH=ml/LP_ocr_nano_62.pt
CONFIDENCE_THRESHOLD=0.5
IOU_THRESHOLD=0.45

# Timezone
TIMEZONE=Asia/Ho_Chi_Minh

# CORS — frontend origin
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
