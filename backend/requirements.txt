fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
supabase>=2.4.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
aiofiles>=23.2.1
httpx>=0.27.0

# Computer Vision
opencv-python-headless>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.66.0
seaborn>=0.13.0

# ML / YOLO
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0

# Image processing
Pillow>=10.0.0

# OCR / Fuzzy matching
python-Levenshtein>=0.21.0

# Logging (already in stdlib but good practice to pin)
# logging — stdlib

# Development only (uncomment if needed)
# pytest>=7.0.0
# httpx>=0.27.0  # for test client
